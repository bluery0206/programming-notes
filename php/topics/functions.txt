
FUNCTIONS
    function funcName(
        dataType paramOne, 
        dataType paramTwo=defaultValue, 
        ..
    ): outputDataTypeOne | outputDataTypeTwo 
    {
        ...code...
    }

    Return DataTypes
        bool
        int
        string
        mixed
        object
        array
        null

        ClassNames

SHORT-HAND ONE-LINER
    fn($arg) => ;

    ie
        $names = ["Fiona", "Kal'tsit"];
        array_map(fn($namee)=>strtolower($name), $names);